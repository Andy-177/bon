# BON (Binary Object Notation) 协议规范

**版本**: v1.0  
**设计目标**: 轻量级、自描述、支持任意精度的二进制序列化格式

---

## 1. 概述

BON 是一种二进制序列化协议，支持多种数据类型，包括 `null`、布尔、整数（智能编码，支持任意精度）、浮点数（双精度）、字符串、二进制数据、数组、对象和高精度小数。所有数据项均采用 TLV（Type-Length-Value）结构，支持嵌套和复杂数据结构。

---

## 2. 整体结构

```
+--------+--------+------------------+
| 魔数    | 类型   | 载荷             |
| (3字节) | (1字节)| (变长)           |
+--------+--------+------------------+
```

### 2.1 魔数 (Magic Number)

- **固定值**: `0x62 0x6F 0x6E` (ASCII: "bon")
- **位置**: 文件/流开头
- **大小**: 3 字节

### 2.2 根类型

- **允许值**: `0x05` (ARRAY) 或 `0x06` (OBJECT)
- **位置**: 魔数之后
- **大小**: 1 字节

### 2.3 载荷

- 根类型的完整 TLV 数据（见 4.4）

---

## 3. 数据类型定义

| 类型标记 | 类型名称 | Python 类型 | 长度字段 | 数据大小 | 说明 |
|---------|---------|------------|---------|---------|------|
| `0x00` | NONE | `None` | **无** | 0 | 空值 |
| `0x01` | BOOL | `bool` | **无** | 1 | 布尔值（0x00=false, 0x01=true） |
| `0x02` | NUMBER | `int` | **模式标记 (1字节)** | **变长** | 任意精度整数（智能编码，自动选择最优编码模式） |
| `0x03` | FLOAT | `float` | **无** | 8 | IEEE 754 双精度浮点数（小端） |
| `0x04` | STRING | `str` | **有 (uint64)** | **变长** | UTF-8 编码字符串 |
| `0x05` | ARRAY | `list` | **有 (uint64)** | **变长** | 有序元素列表 |
| `0x06` | OBJECT | `dict` | **有 (uint64)** | **变长** | 键值对集合 |
| `0x07` | BLOB | `bytes` | **有 (uint64)** | **变长** | 原始二进制数据 |
| `0x08` | DECIMAL | `Decimal` | **无** | 16 | 高精度小数（int64 系数 + uint64 小数位数） |

---

## 4. 类型格式详解

### 4.1 固定长度类型（无长度字段）

#### NONE (0x00)
```
+--------+
|  0x00  |
+--------+
```
- **总大小**: 1 字节

#### BOOL (0x01)
```
+--------+--------+
|  0x01  |  value |
+--------+--------+
```
- **value**: 1 字节，`0x00`=false，`0x01`=true
- **总大小**: 2 字节

#### FLOAT (0x03)
```
+--------+--------+--------+--------+--------+--------+--------+--------+--------+
|  0x03  |  double (IEEE 754, 小端)                                               |
+--------+--------+--------+--------+--------+--------+--------+--------+--------+
```
- **总大小**: 9 字节

#### DECIMAL (0x08)
```
+--------+--------+--------+--------+--------+--------+--------+--------+--------+
|  0x08  |  coefficient (int64, 有符号, 小端)      |  scale (uint64, 无符号, 小端) |
+--------+--------+--------+--------+--------+--------+--------+--------+--------+
```
- **系数 (coefficient)**: 8 字节有符号整数，为去掉小数点后的整数，符号随系数
- **小数位数 (scale)**: 8 字节无符号整数，为小数点后的位数
- **取值**: `value = coefficient × 10⁻ˢᶜᵃˡᵉ`
- **示例**: `"123.456"` → 系数=123456，位数=3；`"-0.456"` → 系数=-456，位数=3；`"1.005"` → 系数=1005，位数=3；`"1.50"` → 系数=150，位数=2
- **总大小**: 17 字节

---

### 4.2 NUMBER (0x02) — 智能编码

NUMBER 使用**模式标记**自动选择最优编码，在类型标记后插入1字节模式标记，根据数值大小自动选择最紧凑的表示方式。用户层面表现为任意精度整数，底层对编码透明。

#### 固定宽度模式（模式 0x00–0x03, 0x05–0x08）

```
+--------+--------+--------+
|  0x02  |  mode  |  data  |
+--------+--------+--------+
```

无额外长度字段，数据大小由模式标记确定。

#### 变长模式（模式 0x04，任意精度）

```
+--------+--------+--------+--------+--------+--------+--------+--------+--------+
|  0x02  |  0x04  |  length (uint64)             |  data (变长)                   |
+--------+--------+--------+--------+--------+--------+--------+--------+--------+
```

用于超出 uint64 范围的任意精度整数，数据为最小字节数小端补码。

#### 模式标记定义

| 标记 | 模式 | 数据大小 | 数值范围 |
|------|------|---------|---------|
| 0x00 | uint8 | 1 字节 | 0 ~ 255 |
| 0x01 | uint16 | 2 字节 | 0 ~ 65535 |
| 0x02 | uint32 | 4 字节 | 0 ~ 4294967295 |
| 0x03 | uint64 | 8 字节 | 0 ~ 18446744073709551615 |
| 0x04 | varint | 变长（含 uint64 长度前缀） | 任意精度 |
| 0x05 | -uint8 | 1 字节 | -1 ~ -256 |
| 0x06 | -uint16 | 2 字节 | -1 ~ -65536 |
| 0x07 | -uint32 | 4 字节 | -1 ~ -4294967296 |
| 0x08 | -uint64 | 8 字节 | -1 ~ -18446744073709551616 |

> 模式标记 0x09–0xFF 为**保留值**，不得使用。解码器遇到保留模式标记时应报错。

#### 编码规则

编码器**自动**选择最小的固定宽度模式：
- **n ≥ 0**: 依次尝试 uint8 → uint16 → uint32 → uint64，选择第一个能容纳 n 的模式
- **n < 0**: 依次尝试 -uint8 → -uint16 → -uint32 → -uint64，选择第一个能容纳 n 的模式
- **超出 uint64 范围**: 使用 varint（任意精度变长补码）

解码器根据模式标记**确定性**还原数值。

#### -uint 编码说明

-uint 模式以**无符号整数存储绝对值减一**，即存储值 `V` 代表实际值 `-(V + 1)`：
- `-uint8`:  存储 `V`（1 字节），V ∈ [0, 255]，实际值 ∈ [-1, -256]
- `-uint16`: 存储 `V`（2 字节），V ∈ [0, 65535]，实际值 ∈ [-1, -65536]
- `-uint32`: 存储 `V`（4 字节），V ∈ [0, 4294967295]，实际值 ∈ [-1, -4294967296]
- `-uint64`: 存储 `V`（8 字节），V ∈ [0, 18446744073709551615]，实际值 ∈ [-1, -18446744073709551616]

-uint 相比有符号整数的优势：不浪费一半位数给符号位，全部位数都用于表示数值大小（绝对值），负数表示更紧凑。

#### varint 模式

varint（模式 0x04）采用**最小字节数小端补码**，用于超出 uint64 范围的任意精度整数：
- 字节长度取能完整表示该数的最小字节数（预留符号位，最高位为符号位）
- 正数：符号位为 0，直接小端字节
- 负数：补码表示，符号位为 1
- 长度字段：varint 数据前有 uint64 长度前缀

#### 示例

| 数值 | 模式 | 存储值 | 编码（十六进制） |
|------|------|--------|----------------|
| `0` | uint8 (0x00) | 0 | `02 00 00` |
| `1` | uint8 (0x00) | 1 | `02 00 01` |
| `255` | uint8 (0x00) | 255 | `02 00 FF` |
| `256` | uint16 (0x01) | 256 | `02 01 00 01` |
| `65535` | uint16 (0x01) | 65535 | `02 01 FF FF` |
| `65536` | uint32 (0x02) | 65536 | `02 02 00 00 01 00` |
| `-1` | -uint8 (0x05) | 0 | `02 05 00` |
| `-256` | -uint8 (0x05) | 255 | `02 05 FF` |
| `-257` | -uint16 (0x06) | 256 | `02 06 00 01` |
| `-12345` | -uint16 (0x06) | 12344 | `02 06 38 30` |

---

### 4.3 变长类型（带 uint64 长度字段）

以下类型在类型标记后都跟一个 **uint64** 长度字段（小端序）。

#### STRING (0x04)
```
+--------+--------+--------+--------+--------+--------+--------+--------+--------+
|  0x04  |  length (uint64)             |  UTF-8 数据 (变长)                    |
+--------+--------+--------+--------+--------+--------+--------+--------+--------+
```
- **长度**: UTF-8 编码后的字节数（uint64）
- **数据**: UTF-8 编码字符串

#### BLOB (0x07)
```
+--------+--------+--------+--------+--------+--------+--------+--------+--------+
|  0x07  |  length (uint64)             |  原始二进制数据 (变长)                |
+--------+--------+--------+--------+--------+--------+--------+--------+--------+
```
- **长度**: 数据的字节数（uint64）
- **数据**: 原始二进制数据

---

### 4.4 复合类型

#### ARRAY (0x05)
```
+--------+--------+--------+--------+--------+--------+--------+--------+--------+
|  0x05  |  total_len (uint64)          |  elem_count (uint64)                  |
+--------+--------+--------+--------+--------+--------+--------+--------+--------+
|  元素1 (TLV)                                                                    |
+--------+--------+--------+--------+--------+--------+--------+--------+--------+
|  元素2 (TLV)                                                                    |
+--------+--------+--------+--------+--------+--------+--------+--------+--------+
|  ...                                                                            |
+--------+--------+--------+--------+--------+--------+--------+--------+--------+
```
- **total_len**: 所有元素 TLV 的总字节数（uint64）
- **elem_count**: 元素个数（uint64）
- **元素**: 每个元素都是完整的 TLV

#### OBJECT (0x06)
```
+--------+--------+--------+--------+--------+--------+--------+--------+--------+
|  0x06  |  total_len (uint64)          |  pair_count (uint64)                  |
+--------+--------+--------+--------+--------+--------+--------+--------+--------+
|  键1 (TLV)                                                                      |
+--------+--------+--------+--------+--------+--------+--------+--------+--------+
|  值1 (TLV)                                                                      |
+--------+--------+--------+--------+--------+--------+--------+--------+--------+
|  键2 (TLV)                                                                      |
+--------+--------+--------+--------+--------+--------+--------+--------+--------+
|  值2 (TLV)                                                                      |
+--------+--------+--------+--------+--------+--------+--------+--------+--------+
|  ...                                                                            |
+--------+--------+--------+--------+--------+--------+--------+--------+--------+
```
- **total_len**: 所有键值对 TLV 的总字节数（uint64）
- **pair_count**: 键值对个数（uint64）
- **键**: 完整的 TLV，可以是任意类型
- **值**: 完整的 TLV，可以是任意类型

---

## 5. 类型标记汇总

| 标记 | 类型 | 长度前缀 | Python 类型（键） | Python 类型（值） |
|------|------|---------|-----------------|-----------------|
| 0x00 | NONE | 无 | `None` | `None` |
| 0x01 | BOOL | 无 | `bool` | `bool` |
| 0x02 | NUMBER | 模式标记 | `int` | `int` |
| 0x03 | FLOAT | 无 | `float` | `float` |
| 0x04 | STRING | uint64 | `str` | `str` |
| 0x05 | ARRAY | uint64×2 | `list` | `list` |
| 0x06 | OBJECT | uint64×2 | `dict` | `dict` |
| 0x07 | BLOB | uint64 | `bytes` | `bytes` |
| 0x08 | DECIMAL | 无 | `Decimal` | `Decimal` |

---

## 6. 完整示例

### 6.1 BON 数据示例

```python
# 根类型为 OBJECT（0x06）
{
    "null_field": None,
    "bool_field": True,
    "number_field": -12345,    # 底层自动选择 -uint16 编码
    "uint_field": 12345,        # 底层自动选择 uint16 编码
    "float_field": 3.14159,
    "string_field": "Hello World",
    "blob_field": b'\x00\x01\x02\x03',
    "decimal_field": Decimal("-123.456"),
    "array_field": [1, "two", 3.0, True],
    "nested_object": {"inner": "value"}
}

# 根类型为 ARRAY（0x05）— 同样合法
[1, "two", 3.0, True]
```

### 6.2 十六进制转储

```
偏移  内容                                      说明
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
0x00  62 6F 6E                                魔数 "bon"
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
根对象 TYPE_OBJECT (0x06)
0x03  06                                     类型标记
0x04  93 00 00 00 00 00 00 00                载荷总长度 = 147
0x0C  04 00 00 00 00 00 00 00                键值对数 = 4
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
第1对: "null_field" -> None
0x14  04                                     键类型 STRING
0x15  0A 00 00 00 00 00 00 00                键长度 = 10
0x1D  6E 75 6C 6C 5F 66 69 65 6C 64           "null_field"
0x27  00                                     值类型 NONE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
第2对: "number_field" -> -12345
0x28  04                                     键类型 STRING
0x29  0C 00 00 00 00 00 00 00                键长度 = 12
0x31  6E 75 6D 62 65 72 5F 66 69 65 6C 64    "number_field"
0x3D  02                                     值类型 NUMBER
0x3E  06                                     模式 = -uint16
0x3F  38 30                                  存储值 = 12344 → 实际 -12345
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
第3对: "decimal_field" -> -123.456
0x41  04                                     键类型 STRING
0x42  0D 00 00 00 00 00 00 00                键长度 = 13
0x4A  64 65 63 69 6D 61 6C 5F 66 69 65 6C 64 "decimal_field"
0x57  08                                     值类型 DECIMAL
0x58  C0 1D FE FF FF FF FF FF                系数 = -123456
0x60  03 00 00 00 00 00 00 00                小数位数 = 3
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
第4对: "array_field" -> [1, "two", 3.0, True]
0x68  04                                     键类型 STRING
0x69  0B 00 00 00 00 00 00 00                键长度 = 11
0x71  61 72 72 61 79 5F 66 69 65 6C 64       "array_field"
0x7C  05                                     值类型 ARRAY
0x7D  1A 00 00 00 00 00 00 00                总长度 = 26
0x85  04 00 00 00 00 00 00 00                元素个数 = 4
0x8D  02 00 01                               元素1: NUMBER(1) [uint8]
0x90  04 03 00 00 00 00 00 00 00 74 77 6F   元素2: STRING("two")
0x9C  03 00 00 00 00 00 00 08 40             元素3: FLOAT(3.0)
0xA5  01 01                                  元素4: BOOL(true)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

### 6.3 ARRAY 根类型示例

数据：`[1, "two", 3.0, True]`

```
偏移  内容                                      说明
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
0x00  62 6F 6E                                魔数 "bon"
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
根类型 TYPE_ARRAY (0x05)
0x03  05                                     类型标记
0x04  1A 00 00 00 00 00 00 00                元素数据总长度 = 26
0x0C  04 00 00 00 00 00 00 00                元素个数 = 4
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
元素1: NUMBER(1)
0x14  02                                     值类型 NUMBER
0x15  00                                     模式 = uint8
0x16  01                                     data = 1
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
元素2: STRING("two")
0x17  04                                     值类型 STRING
0x18  03 00 00 00 00 00 00 00                长度 = 3
0x20  74 77 6F                                "two"
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
元素3: FLOAT(3.0)
0x23  03                                     值类型 FLOAT
0x24  00 00 00 00 00 00 08 40                3.0（IEEE 754，小端）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
元素4: BOOL(true)
0x2C  01 01                                   值类型 BOOL, true
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

---

## 7. 实现注意事项

### 7.1 整数编码
- NUMBER 使用智能编码，自动选择最紧凑的固定宽度模式（uint8/uint16/uint32/uint64 或 -uint8/-uint16/-uint32/-uint64）
- -uint 模式存储 `V = |n| - 1`，全部位数用于表示绝对值，不浪费一半位数给符号位
- 超出 uint64 范围时自动切换 varint（任意精度最小字节补码）
- 编解码模式选择对用户透明，任意整数均可存取

### 7.2 键的约束
- 任意类型都可以作为键（包括 ARRAY、OBJECT）
- 键与值的 TLV 编码规则相同，无任何类型限制

### 7.3 长度字段
- STRING、BLOB、ARRAY、OBJECT 使用 **uint64**（8 字节）长度前缀
- NUMBER 仅在 varint 模式（0x04）使用 uint64 长度前缀，固定宽度模式无长度字段
- 支持最大 2⁶⁴-1 字节的数据

### 7.4 字节序
- **小端序** (Little-Endian)

---

## 8. MIME 类型建议

```
application/x-bon
```

---

## 9. 版本历史

| 版本 | 日期 | 变更 |
|------|------|------|
| v1.0 | 2026-07-26 | 初始版本 |

---

**协议规范结束**
